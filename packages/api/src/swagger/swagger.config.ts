import { ISwaggerConfig } from './swagger.types';

export const SwaggerConfig: ISwaggerConfig = {
  title: 'JIkaheimo Template',
  description: 'Template',
  version: '1.0',
  tags: ['post'],
};

export default SwaggerConfig;
